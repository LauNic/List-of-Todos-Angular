import { MomentUtcDateAdapter } from './moment-utc-date-adapter';

describe('MomentUtcDateAdapter', () => {
  it('should create an instance', () => {
    expect(new MomentUtcDateAdapter()).toBeTruthy();
  });
});
