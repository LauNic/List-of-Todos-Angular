import { AuthenticationBean } from './authentication-bean';

describe('AuthenticationBean', () => {
  it('should create an instance', () => {
    expect(new AuthenticationBean()).toBeTruthy();
  });
});
